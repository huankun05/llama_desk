# 更新日志（CHANGELOG）

> 本文件记录**面向用户**的版本变更，按语义化版本（SemVer）分节。
> 约定：每轮开发完成后，先把用户可感知的改动记入 `[Unreleased]` 节；
> 发版时（打 tag `vX.Y.Z` + 发布 Release）把 Unreleased 内容整理为新版本小节。
> 版本号规则：不兼容改动 / 大功能 → 升次版本号（1.1.0）；修 bug / 小改进 → 升修订号（1.0.1）。
> 开发过程细节（根因分析、验收记录）仍记在 `docs/roadmap-v2.md` 与 `docs/notes/`，本文只留结论。

## [Unreleased]

（暂无——下一轮开发的改动先记到这里）

## [1.0.0] - 2026-09-26

首个正式发布版本。

### 应用壳

- 单文件 `llama-desk.exe` 应用壳（Tauri2）：启动即托管 llama-server + manager 后端 + WebUI，退出时自动清理全部服务进程（含端口被外部占用场景的识别与回收）。
- 首次运行向导：缺什么引导什么——一键下载最新 llama.cpp 引擎（带实时进度）、粘贴本地 `llama-server.exe` 路径直接复用（`--version` 冒烟校验）、Python 检测与指引，完成后自动生成 `config.json` 并重启。
- 系统托盘：加载/卸载模型、重启服务、打开日志等常用操作入口。

### WebUI

- 全站中文本地化（overlay 词典方案，不改动上游界面文件，升级 WebUI 不丢翻译）。
- 模型库：Hugging Face 在线搜索与下载（量化类型筛选、下载前显示真实文件大小、分页、磁盘空间检查）、模型标签/收藏/备注、删除进 Windows 回收站（可反悔）。
- 显存可行性徽章：加载前预估模型在当前显卡能否跑得动；KV 缓存可信度提示。
- 模型加载进度、一键性能基准测试。
- 设置页（聊天 / 关于应用等独立分页），暗色主题适配。

### 更新系统

- **llama.cpp 引擎更新**（应用内一键完成）：检查 ggml-org/llama.cpp 官方新版本 → 确认下载（实时进度条）→ 自动备份 → SHA256 校验 → 冒烟测试 → 自动重启服务；全程分阶段提示。
- **应用壳更新**：独立检查通道，发现新版本显示「当前版本 → 最新版本」对比，引导前往 Releases 页下载替换（数据全部存 exe 之外，替换不丢配置/模型/聊天记录）。
- 启动时后台静默检查（仅提示，不自动下载）。

### 已知限制

- 应用壳更新为手动替换 exe（自动自更新在计划中）。
- Release 附件暂未提供 SHA256SUMS 校验文件。
